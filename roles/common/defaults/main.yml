---

public_keys:
  - username: ravage
    keys:
      - "{{ lookup('file', 'ssh/ravage.pub') }}"

users:
  ravage:
    password: "{{ lookup('password', '../../../credentials/ravage.pwd encrypt=sha512_crypt') }}"
    shell: '/bin/bash'

ssh:
  port: 22

sudo:
  group: wheel
  users:
    - ravage

ruby:
  version: "2.2.0"
  folder: "2.2"

cache_time: 3600